import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)

import urequests as requests
import network


def connect_wlan(ssid, password):
    sta_if = network.WLAN(network.STA_IF)
    ap_if = network.WLAN(network.AP_IF)
    sta_if.active(True)
    ap_if.active(False)

    if not sta_if.isconnected():
        print("Connecting to WLAN ({})...".format(ssid))
        sta_if.active(True)
        sta_if.connect(ssid, password)
        while not sta_if.isconnected():
            print("Connecting...")
            pass

    print("Connected to {}.".format(ssid))
    return True


SSID = "test_div"
PASSWORD = "00000000"
try:
    connect_wlan(SSID, PASSWORD)
except:
    print("WLAN connection failed.")

a = requests.get('https://github.com/UF4OVER/esp32-s3')
print(a.text)
